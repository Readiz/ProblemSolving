#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    scanf("%d", &N);

    for(int v = N / 4; v > 0; --v) {
        printf("long ");
    }
    printf("int\n");
    return 0;
}